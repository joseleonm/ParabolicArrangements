#!/bin/bash
set -e

# Precalienta Sage EN TIEMPO DE BUILD (la imagen ya queda lista)
# Esto evita que el primer import tarde demasiado en runtime.
sage -c 'print("Sage present:", version()); _=1+1'

python - <<'PY'
from sageall import RootSystem
print("Python puede importar sageall y RootSystem OK")
PY
